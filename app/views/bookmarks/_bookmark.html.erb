<div class="item">
<h4><%= link_to bookmark.url, bookmark.url %></h4>
  <div class="well"> 
      <%= link_to "Edit", edit_topic_bookmark_path( bookmark.topic, bookmark), class: 'btn btn-info btn-xs' %>
      <%= link_to "Delete", topic_bookmark_path(bookmark.topic, bookmark), method: :delete, data: {confirm: 'Are you sure you want to delte this Topic?'}, class: 'btn btn-danger btn-xs' %>
  </div>
</div>
